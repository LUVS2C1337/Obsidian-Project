# 2026-07-12 工作记录

## 项目概况（D:\Obsidian Project）
- Obsidian 个人知识库，采用 PARA 结构；核心是「四工具协同学习系统」：Claude Code（中枢）+ 飞书（执行）+ Obsidian（记忆）+ NotebookLM（深度阅读）。
- 集成方式：`obsidian-local-rest-api` 插件让 Claude 经 MCP 直接读写 Obsidian；`.claude/` 存桥接说明/对话索引/提示词库。

## 项目约定（已确认）
- 项目笔记放 `1-Projects/p-项目名/`；领域 `2-Areas/a-领域名/`；资源 `3-Resources/主题/`。
- 所有 vault 笔记必须含 frontmatter：`date` / `tags` / `aliases`。
- 重复笔记用「单一信息源 + 重定向桩」去重。

## 今日改动（用户授权「全部进行」）
1. frontmatter 归一化：为全库 vault 笔记补齐 date/tags/aliases（脚本批量，排除 .templates/占位文件/README，README 在重写时自带）。
2. 将根目录 `p-ai-agent/` 用 git mv 迁移到 `1-Projects/p-ai-agent/`，并修正 `6-WorkBuddy面试准备.md` 内的 wikilink。
3. 启用插件：`.obsidian/community-plugins.json` 加入 dataview / templater-obsidian / quickadd / calendar。
4. 重写 `README.md` 反映最新结构（含 p-ai-agent、p-备考AWS），移除占位链接。
5. `git rm` 删除 Obsidian 默认占位笔记 `创建链接.md`、`欢迎.md`（删除前确认无入链）。
6. 为 `0-Inbox/收件箱.md` 增加 Dataview「待整理」看板查询。
- 已提交：commit `f1f399d`（27 files changed）。

## 第二轮改动（用户确认 base 查看不了，要求落地额外改进）
7. 删除 `p-ai-agent/` 下 3 个重定向桩（6-WorkBuddy面试准备.md / 7-面试反问问题.md / 面试反问问题清单.md），均无 wikilink 入链，安全删除。
8. 删除无法查看的 `.base` 视图（📊 知库总览.base / 🕒 最近更新.base），改用 Dataview 重写为 `📊 知库总览.md` / `🕒 最近更新.md`（兼容 1.9 以下版本，解决查看不了的问题）。
9. 新增 `1-Projects/p-ai-agent/🎯 AI Agent 学习.md` 项目主页（status: active），自动被 `项目索引.md` 的 Dataview 收录；同时作为知库主页「当前活跃」导航入口。
10. 知库主页增加「AI Agent 学习」与「总览视图」（链接两个 Dataview 页面）入口。
11. 同步更新 README 目录树（去掉桩/base、加新主页与 Dataview 视图）、将 .base 提示改为 Dataview 替代说明。
- 已提交：commit `1e0fa5d`（10 files changed）。

## 备注
- `2-Areas/面试反问问题清单.md` 为面试反问问题单一信息源；原 p-ai-agent 下 3 个桩已全部删除收敛。
- base 视图问题已解决：改用 Dataview 重写，无需升级 Obsidian。
- git 提交时排除 `.workbuddy/`（工具记忆目录）、`.obsidian/workspace.json`（运行时状态）、插件 data.json，只提交知识库本体改动。
- 本次为在用户明确授权后首次对知识库本体做改动（此前仅只读研究）。
